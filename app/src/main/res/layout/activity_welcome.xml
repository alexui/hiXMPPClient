<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".WelcomeActivity">

    <Button
        android:id="@+id/registerButtonMain"
        style="@style/mainActivityButton"
        android:layout_above="@+id/spaceBeforeLoginButtonMain"
        android:text="register"
        />

    <Space
        android:id="@+id/spaceBeforeRegisterButtonMain"
        android:layout_width="80px"
        android:layout_height="200px"
        android:layout_above="@id/registerButtonMain"
        android:layout_centerHorizontal="true" />

    <ImageView
        android:id="@+id/logoImageViewMain"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:layout_above="@id/spaceBeforeRegisterButtonMain"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        android:clickable="true"
        android:scaleType="fitCenter" />

    <Space
        android:id="@+id/spaceBeforeLoginButtonMain"
        android:layout_width="80px"
        android:layout_height="20px"
        android:layout_above="@+id/loginButtonMain"
        android:layout_centerHorizontal="true" />

    <Button
        android:id="@+id/loginButtonMain"
        style="@style/mainActivityButton"
        android:layout_alignParentBottom="true"
        android:text="log in" />


</RelativeLayout>
